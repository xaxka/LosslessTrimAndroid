<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- 直路径唯一管线：SAF(saf: 协议)只写描述符上 -movflags +faststart
         收尾回移数据不可靠，部分设备产出缺 moov 的坏 MP4，SAF 数据通道已彻底
         移除。须授予"所有文件"权限后 ffmpeg 走普通文件 I/O；未授予时直接
         报错引导授权（无回退通道）。无 INTERNET 权限：应用为纯本地工具，
         无任何网络访问（更新需自行关注 GitHub Releases）。 -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <!-- Android 10 及以下：传统外部存储权限（配合 requestLegacyExternalStorage） -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />

    <!-- 不设 android:largeHeap：它是修"全尺寸帧 OOM"时代（33MB/张）的兜底，真根因
         （小图化 / 双层缓存 / 磁盘溢写 / 会话历史压缩）早已落地。保留它只会让
         ART 堆上限翻倍，长批处理期间进程可膨胀数百 MB，挤占系统内存 →
         LMK 杀掉用户的后台应用（音乐/IM/浏览器）。 -->
    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:requestLegacyExternalStorage="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LosslessTrim">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".trim.TrimService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>
